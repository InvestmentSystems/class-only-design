language: python
python:
  - "3.7"
env:
  - TOXENV=py35
  - TOXENV=py36
  - TOXENV=py37
install:
  - pip install -U tox codecov

script:
  - tox

after_success:
  - codecov

addons: #Added to get python 3.5
  apt:
    sources:
      - deadsnakes
    packages:
      - python3.5
      - python3.6
